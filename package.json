{
  "name": "not-balatro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix app/backend install && npm --prefix frontend install",
    "dev": "start \"Not-Balatro API\" cmd /k \"npm --prefix app/backend run dev\" && start \"Not-Balatro Frontend\" cmd /k \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix app/backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  }
}
